{
    let body = html_nested!(
        <PopoverBody
            header={html!("Popover Title")}
            footer={html!("Some footer content.")}
        >
            {"This is just an example popover."}
        </PopoverBody>
    );

    html!( <PopoverPopup orientation={Orientation::Left} {body}/> )
}